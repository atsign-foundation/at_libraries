name: at_lookup
description: Library for executing at commands on a secondary server.
version: 1.0.0
repository: https://github.com/atsign-foundation/at_libraries
homepage: https://atsign.dev

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  path: ^1.6.0
  crypton: ^1.1.0
  at_utils:
    git:
      url: https://github.com/atsign-foundation/at_tools.git
      path: at_utils
      ref: master
      version: ^1.0.0

dev_dependencies:
  pedantic: ^1.8.0
  test: ^1.6.0
