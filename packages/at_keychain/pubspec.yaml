name: at_keychain
description: at_keychain is a library which provides common interfaces for storing the atPlatform's cryptographic keys.
version: 0.1.0
repository: https://github.com/atsign-foundation/at_libraries
homepage: https://docs.atsign.com

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  better_cryptography: ^1.0.0+1
  at_utils: ^3.2.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
